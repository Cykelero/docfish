<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>
		{{#if pageName}}
			{{{pageName}}} — {{{productName}}}
		{{else}}
			{{{productName}}}
		{{/if}}
	</title>
	<meta name="viewport" content="width = device-width, initial-scale = 1, user-scalable = no">
	<link href="resources/main.css" type="text/css" rel="stylesheet">
	<link href="resources/580-.css" type="text/css" rel="stylesheet" media="(max-width: 580px)">
	<link href="resources/highlightjs.css" type="text/css" rel="stylesheet">
	<script src="resources/main.js" type="text/javascript"></script>
</head>
<body>
	<nav>
		<a href="./" style="color: {{{productThemeColor}}}">
			{{#if productLogoPath}}
				<img class="product-logo" src="{{{productLogoPath}}}" alt="">
			{{/if}}
			<span class="product-name">{{{productName}}}</span>
			{{#if productVersion}}
				<span class="product-version">{{{productVersion}}}</span>
			{{/if}}
		</a>
	</nav>
	
	<div class="page">
		{{{pageHTML}}}
	</div>
</body>
</html>
